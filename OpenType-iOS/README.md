# OpenType iOS (Planned)

iOS app and keyboard extension — planned for Phase C (v1.1.0+).

## Status: Not yet implemented

The macOS native app (Swift 5.9, AppKit + SwiftUI) is the current priority.
iOS development will begin after the Shared Swift Package extraction is complete.

## Planned structure

- **App/** — iOS host app with SwiftUI
- **KeyboardExtension/** — Custom keyboard extension for system-wide voice input
- **UI/** — Shared SwiftUI components across app and extension

See `IMPROVEMENT_PLAN.md` Phase C for details.
