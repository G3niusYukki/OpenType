<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow execution of JIT-compiled code (required for Python) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Allow unsigned executable memory -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Disable library validation (required for PyObjC) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Allow debugging (for development) -->
    <key>com.apple.security.get-task-allow</key>
    <true/>

    <!-- File access -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Network access for auto-updater -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- Process management for controlling OpenType -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>
</dict>
</plist>
